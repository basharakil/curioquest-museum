{
  "name": "curioquest-museum",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A training app for learning GitHub workflows end to end.",
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "validate:dist": "node scripts/validate-dist.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
